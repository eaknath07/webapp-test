revolte:
  version: '1.0'
  project: test-proj
  appName: react-service
  env:
    qa:
      branch: qa
      services:
        rid_1:
          type: WEB_APP
          name: web-app-service
          entryPoint: index.js
          properties:
            indexPage: index.html
            errorPage: error.html
            enableCdn: true
            variables:
              BASE_URL: /api/v1
              VITE_INFO: "#{info}"
